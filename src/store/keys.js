export const MONSTER = "monster"; // XXX might replace this with Symbol('monster') to guarantee uniqueness... except that's probably a bad idea because so much else just uses regular 'monster'
export const CURRENT_INFO = "currentInfo"; // TODO switch to using my new structure
